streamlit>=1.38
python-docx>=1.1
python-pptx>=1.0
pandas>=2.2
pymupdf>=1.24
pdfplumber>=0.11
openai>=1.40
anthropic>=0.34
google-generativeai>=0.7
python-dotenv>=1.0
pydantic>=2.7
Pillow>=10.3
matplotlib>=3.9
msal>=1.28
requests>=2.31
openpyxl>=3.1

# --- Added for the SaaS build (accounts, database, billing) ---
sqlalchemy>=2.0
psycopg2-binary>=2.9
bcrypt>=4.1
itsdangerous>=2.2
extra-streamlit-components>=0.1.71
stripe>=10.0

# --- Added for background jobs (multi-user concurrency) ---
redis>=5.0
rq>=1.16

# --- Added for the scanned-PDF OCR fallback (see modules/document_processor.py) ---
# Also needs the tesseract-ocr SYSTEM package at runtime -- installed on
# Railway via nixpacks.toml's aptPkgs (see that file). Without the binary,
# the app still runs; scanned PDFs get a clear "this brief looks scanned"
# message instead of OCR.
pytesseract>=0.3.10
